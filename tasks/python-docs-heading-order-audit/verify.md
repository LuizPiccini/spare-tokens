# Verification

A human reviewer should check:

- every listed URL is a public `docs.python.org` page
- the accessibility tool and version are named
- each finding has a selector, DOM snippet, or screenshot
- recommended fixes are narrow and do not change Python documentation meaning

Preferred output: `artifacts/report.md` with an issue draft.
