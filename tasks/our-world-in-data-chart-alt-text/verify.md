# Verification

A human reviewer should check:

- each chart URL is included
- the proposed text matches the visible chart
- the text avoids unsupported causal claims
- the text is concise enough to be usable as alt text or a caption seed

Preferred output: a table with URL, current description if present, proposed text, and rationale.

